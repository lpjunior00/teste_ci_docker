server:
  port: 8888
spring:
  application:
    name: junior-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/lpjunior00/config-server
          default-label: main
          search-paths:
          - 'greeting-service*'